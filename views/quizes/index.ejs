
<form method="get" action="/quizes">        
  <p>Buscar
  <input type="text" name="search" placeholder="Buscar pregunta..."/>
  <input type="submit" value="Enviar">
</form>
<p>

<%=search%>

<p>
<table>
  <% var i; t=""; for (i=0; i < quizes.length; i++) { %>        
     <tr>
       <% if(t!=quizes[i].tema) {%>
          
            <td><%= quizes[i].Tema.descripcion %></td>
       </tr>
       <tr>
       <%} %>
       <td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
       
       <td><a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
       <td>
           <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
                <button type="submit" onClick="return confirm('¿Desea borrar la pregunta: <%= quizes[i].pregunta %> ?');">borrar</button>
           </form>
       </td>
     </tr> 
     
  <% t=quizes[i].tema;} %>
</table>

<p>
<a href="/quizes/new"><button type="button">Crear pregunta</button></a>